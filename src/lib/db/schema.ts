// Drizzle schema.
//
// Intentionally minimal for Phase 0. No product/domain tables yet —
// those land in Phase 2. Keeping a no-op export so the schema module,
// drizzle-kit, and the db client all resolve cleanly.

export {};
